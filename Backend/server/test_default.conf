# Test configuration for Django
SECRET_KEY = django-insecure-test-key-123456789
DEBUG = True
ALLOWED_HOSTS = localhost,127.0.0.1

# Database
DATABASE_ENGINE = django.db.backends.sqlite3
DATABASE_NAME = :memory:

# Email
EMAIL_BACKEND = django.core.mail.backends.locmem.EmailBackend

# JWT Settings
ACCESS_TOKEN_LIFETIME_MINUTES = 60
REFRESH_TOKEN_LIFETIME_DAYS = 1

# CORS
CORS_ALLOWED_ORIGINS = http://localhost:5173,http://127.0.0.1:5173

# Language
LANGUAGE_CODE = fa
TIME_ZONE = Asia/Tehran

# S3 Storage (disabled for tests)
KUBIT_S3_ENDPOINT_URL = 
KUBIT_STORAGE_BUCKET_NAME = 
KUBIT_ACCESS_KEY_ID = 
KUBIT_SECRET_ACCESS_KEY = 
KUBIT_S3_REGION_NAME = 
KUBIT_S3_CUSTOM_DOMAIN = 

# Data root
DATA_ROOT = ./test_data